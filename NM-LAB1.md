# Theory

## 1.1 LU-Преобразование

Начнём с идеи исключения неизвестной.

Возвращаемся к системе:

$$ \begin{cases} 2x_1+x_2=5,\\ 4x_1+3x_2=11. \end{cases} $$

Мы хотим убрать \(x_1\) из второго уравнения. Тогда в нём останется одна неизвестная, которую легко найти.

В первом уравнении перед \(x_1\) стоит \(2\), во втором — \(4\). Поэтому из второго уравнения вычтем первое, умноженное на \(2\):

$$ (4x_1+3x_2)-2(2x_1+x_2)=11-2\cdot5. $$

Раскрываем скобки:

$$ 4x_1+3x_2-4x_1-2x_2=1, $$

следовательно,

$$ x_2=1. $$

Первое уравнение сохраняем. Получили систему:

$$ \begin{cases} 2x_1+x_2=5,\\ x_2=1. \end{cases} $$

Почему можно так делать? Мы заменили второе уравнение разностью двух верных равенств. Преобразование обратимо: если к новому второму уравнению прибавить удвоенное первое, восстановится исходное второе. Поэтому набор решений не изменился.

Теперь подставляем \(x_2=1\) в первое:

$$ 2x_1+1=5 \quad\Rightarrow\quad x_1=2. $$

Это два этапа метода Гаусса:

Прямой ход: исключаем неизвестные из нижних уравнений.
Обратный ход: находим неизвестные снизу вверх, подставляя уже найденные значения.

Посмотрим только на то, что произошло с матрицей коэффициентов:

$$ A= \begin{pmatrix} 2&1\\ 4&3 \end{pmatrix} \quad\xrightarrow{\text{строка 2 минус }2\cdot\text{строка 1}}\quad U= \begin{pmatrix} 2&1\\ 0&1 \end{pmatrix}. $$

Получили верхнюю треугольную матрицу: под главной диагональю стоят нули. Буква \(U\) — от английского upper, «верхний».

А число \(2\), на которое мы умножали первую строку, — множитель исключения. Оно не взялось наугад:

$$ m_{21}= \frac{\text{коэффициент, который хотим обнулить}} {\text{коэффициент, с помощью которого обнуляем}} = \frac{4}{2}=2. $$

Именно такие множители затем сохраняются в матрице \(L\). В этом примере:

$$ L= \begin{pmatrix} 1&0\\ 2&1 \end{pmatrix}. $$

Пока главное уловить связь: \(U\) — результат исключения, а \(L\) хранит множители, использованные при исключении. На следующем шаге проверим, почему их произведение восстанавливает \(A\). Перестановки строк и выбор опорного элемента добавим после этого.

А про \(L\) я действительно перескочил шаг. «Сохраняем множитель» означает буквально: записываем вычисленное число в определённую ячейку отдельной матрицы. Посмотрим, куда и зачем.

Сначала создаём \(L\) как единичную матрицу:

$$ L= \begin{pmatrix} 1&0\\ 0&1 \end{pmatrix}. $$

Одновременно берём копию исходной матрицы — её будем постепенно превращать в \(U\):

$$ U= \begin{pmatrix} 2&1\\ 4&3 \end{pmatrix}. $$

Чтобы обнулить \(4\), вычисляем множитель:

$$ m_{21}=\frac{4}{2}=2. $$

И делаем два отдельных действия:

В \(U\) вычитаем из второй строки удвоенную первую.
В \(L\) записываем множитель \(2\) в ячейку второй строки, первого столбца.

Получается:

$$ U= \begin{pmatrix} 2&1\\ 0&1 \end{pmatrix}, \qquad L= \begin{pmatrix} 1&0\\ \boxed{2}&1 \end{pmatrix}. $$

Почему именно эта ячейка? Мы исключали элемент в позиции \((2,1)\): из строки 2 вычитали строку 1, умноженную на \(2\). В эту же позицию в \(L\) записали использованный множитель.

Теперь главное: почему такая запись позволяет восстановить исходную матрицу?

При исключении мы сделали:

$$ \text{новая строка 2} = \text{исходная строка 2} - 2\cdot\text{строка 1}. $$

Значит, восстановить исходную строку можно так:

$$ \text{исходная строка 2} = 2\cdot\text{строка 1} + \text{новая строка 2}. $$

Именно это выполняет умножение \(LU\):

$$ LU= \begin{pmatrix} 1&0\\ 2&1 \end{pmatrix} \begin{pmatrix} 2&1\\ 0&1 \end{pmatrix} = \begin{pmatrix} 1\cdot2+0\cdot0 & 1\cdot1+0\cdot1\\ 2\cdot2+1\cdot0 & 2\cdot1+1\cdot1 \end{pmatrix} = \begin{pmatrix} 2&1\\ 4&3 \end{pmatrix} =A. $$

Первая строка \(L\), то есть \((1,0)\), говорит: «Возьми первую строку \(U\)».

Вторая строка \(L\), то есть \((2,1)\), говорит: «Возьми две первых строки \(U\) плюс одну вторую»:

$$ 2\cdot(2,\;1)+(0,\;1)=(4,\;3). $$

Поэтому в \(L\) записываем \(2\), а не \(-2\): при исключении вычитали, а при восстановлении прибавляем.

Для матрицы \(3\times3\) без перестановок это устроено так же:

$$ L= \begin{pmatrix} 1&0&0\\ m_{21}&1&0\\ m_{31}&m_{32}&1 \end{pmatrix}. $$

Например, \(m_{32}\) — множитель, с которым вычитаем вторую строку из третьей, чтобы обнулить элемент третьей строки, второго столбца. Его вычисляем по значениям, полученным к этому моменту исключения.

Пока рассматриваем случай без перестановок строк; при выборе опорного элемента добавится правило перестановки уже записанных множителей.

Теперь посмотрим на \(3\times3\): здесь станет видно, как в \(L\) накапливаются несколько множителей. Пока без перестановок строк.

Пусть

$$ A= \begin{pmatrix} 2&1&1\\ 4&3&3\\ 6&5&6 \end{pmatrix}. $$

В начале \(U=A\), а \(L\) — единичная:

$$ L= \begin{pmatrix} 1&0&0\\ 0&1&0\\ 0&0&1 \end{pmatrix}. $$

1. Убираем \(4\) под первым диагональным элементом.

Множитель \(m_{21}=4/2=2\). Из второй строки вычитаем удвоенную первую:

$$ (4,3,3)-2(2,1,1)=(0,1,1). $$

Записываем \(L_{21}=2\).

2. Убираем \(6\) в том же столбце.

Множитель \(m_{31}=6/2=3\). Из третьей строки вычитаем утроенную первую:

$$ (6,5,6)-3(2,1,1)=(0,2,3). $$

Записываем \(L_{31}=3\). Сейчас имеем:

$$ U= \begin{pmatrix} 2&1&1\\ 0&1&1\\ 0&2&3 \end{pmatrix}, \qquad L= \begin{pmatrix} 1&0&0\\ 2&1&0\\ 3&0&1 \end{pmatrix}. $$

Первый столбец обработан: ниже диагонали в нём нули.

3. Убираем \(2\) под вторым диагональным элементом.

Теперь используем обновлённую вторую строку \((0,1,1)\).

Множитель:

$$ m_{32}=\frac{2}{1}=2. $$

Из текущей третьей строки вычитаем удвоенную текущую вторую:

$$ (0,2,3)-2(0,1,1)=(0,0,1). $$

Записываем \(L_{32}=2\). Разложение готово:

$$ \boxed{ L= \begin{pmatrix} 1&0&0\\ 2&1&0\\ 3&2&1 \end{pmatrix}, \qquad U= \begin{pmatrix} 2&1&1\\ 0&1&1\\ 0&0&1 \end{pmatrix} } $$

Посмотрим, как восстанавливается третья строка \(A\). Третья строка \(L\) равна \((3,2,1)\), поэтому в произведении \(LU\) получаем:

$$ 3\cdot\underbrace{(2,1,1)}_{\text{строка 1 в }U} + 2\cdot\underbrace{(0,1,1)}_{\text{строка 2 в }U} + 1\cdot\underbrace{(0,0,1)}_{\text{строка 3 в }U} = (6,5,6). $$

Это исходная третья строка. Все сохранённые множители пригодились.

Обрати внимание на два момента:

Множитель считаем по текущей матрице \(U\): она меняется после каждого исключения.
Единицы на диагонали \(L\) означают, что собственную оставшуюся строку \(U\) берём один раз. В этой схеме мы вычитаем кратные других строк, но саму обрабатываемую строку дополнительно не масштабируем.


Возьмём матрицу:

$$ A= \begin{pmatrix} 0&2\\ 3&4 \end{pmatrix}. $$

Попробуем действовать как раньше: убрать число \(3\) под первым диагональным элементом. Для этого нужен множитель:

$$ m_{21}=\frac{3}{0}. $$

На ноль делить нельзя. Но это ещё не значит, что систему нельзя решить. Просто первой строкой сейчас неудобно пользоваться.

Поменяем строки местами:

$$ \begin{pmatrix} 0&2\\ 3&4 \end{pmatrix} \quad\longrightarrow\quad \begin{pmatrix} 3&4\\ 0&2 \end{pmatrix}. $$

Теперь сверху стоит \(3\), и проблемы нет. Более того, здесь уже получилась верхняя треугольная матрица.

В системе уравнений такая перестановка означает лишь, что мы записали второе уравнение перед первым. При перестановке обязательно переносим и правую часть уравнения. Решение от смены порядка уравнений не меняется.

Теперь введём обозначение \(P\). Это матрица, умножение на которую слева переставляет строки. Для обмена двух строк:

$$ P= \begin{pmatrix} 0&1\\ 1&0 \end{pmatrix}. $$

Посмотрим на произведение:

$$ PA= \begin{pmatrix} 0&1\\ 1&0 \end{pmatrix} \begin{pmatrix} 0&2\\ 3&4 \end{pmatrix} = \begin{pmatrix} 3&4\\ 0&2 \end{pmatrix}. $$

Почему так:

Первая строка \(P\), то есть \((0,1)\), выбирает вторую строку \(A\).
Вторая строка \(P\), то есть \((1,0)\), выбирает первую строку \(A\).

\(PA\) — это обычное произведение матриц, результат которого здесь равен \(A\) с переставленными строками.

Поэтому:

$$ \boxed{PA=LU} $$

читается так:

После перестановки строк исходная матрица равна произведению \(L\) и \(U\).

В нашем примере после обмена ничего исключать не понадобилось, поэтому:

$$ L= \begin{pmatrix} 1&0\\ 0&1 \end{pmatrix}, \qquad U= \begin{pmatrix} 3&4\\ 0&2 \end{pmatrix}. $$

И действительно, \(LU=U=PA\).

Если строки вообще не переставляли, \(P\) — единичная матрица, поэтому \(PA=A\), и остаётся знакомое \(A=LU\).

Пусть система такая:

$$ \begin{cases} 0x_1+2x_2=10,\\ 3x_1+4x_2=14. \end{cases} $$

Когда меняем уравнения местами, получается:

$$ \begin{cases} 3x_1+4x_2=14,\\ 0x_1+2x_2=10. \end{cases} $$

Число \(14\) переехало вместе со своим уравнением, а \(10\) — со своим. Поэтому меняются местами и элементы столбца \(b\):

$$ b=\begin{pmatrix}10\\14\end{pmatrix} \quad\longrightarrow\quad Pb=\begin{pmatrix}14\\10\end{pmatrix}. $$

Если переставить только коэффициенты слева, а правые части оставить на месте, получится другая система. Только это я и хотел уточнить.

Теперь следующий небольшой шаг: как выбирать, какую строку поставить наверх?

Число, с помощью которого обнуляем элементы под ним, называют опорным элементом. Например, здесь опорным будет \(3\):

$$ \begin{pmatrix} \boxed{3}&4\\ 0&2 \end{pmatrix}. $$

В нашей реализации будем выбирать наибольший по модулю элемент текущего столбца среди текущей строки и всех строк ниже. Это называется частичным выбором опорного элемента.

Например, если на первом шаге первый столбец выглядит так:

$$ \begin{pmatrix} 2\\ -8\\ 5 \end{pmatrix}, $$

сравниваем модули: \(2,\ 8,\ 5\). Выбираем \(-8\) и меняем первую и вторую строки целиком.

Зачем выбирать наибольший по модулю? Мы будем делить на него при вычислении множителей. Так избегаем деления на ноль, если в столбце есть ненулевой кандидат, и уменьшаем риск усиления ошибок округления при делении на очень маленькое число. При таком выборе модули множителей исключения не превышают \(1\).

На каждом шаге порядок такой: выбрать опорный элемент → переставить строки, если нужно → вычислить множители → обнулить элементы под опорным. Уже обработанные строки выше текущей больше в выборе не участвуют.

Теперь разберём, как с помощью \(L\) и \(U\) найти решение системы. Ведь пока мы научились раскладывать матрицу, а неизвестные \(x_1,x_2,\ldots\) ещё нужно вычислить.

У нас есть исходная система:

$$ Ax=b. $$

После перестановки уравнений она выглядит так:

$$ PAx=Pb. $$

Поскольку \(PA=LU\), подставляем:

$$ LUx=Pb. $$

Введём временный столбец \(y=Ux\). Это просто обозначение промежуточного результата. Тогда вместо одной системы получаем две, которые решаем по очереди:

$$ \boxed{Ly=Pb} \qquad\text{затем}\qquad \boxed{Ux=y}. $$

Сначала находим \(y\), потом через него — \(x\). Покажу на числах, зачем это удобно.

Возьмём знакомую систему:

$$ \begin{cases} 2x_1+x_2=5,\\ 4x_1+3x_2=11. \end{cases} $$

Теперь применим выбор опорного элемента. В первом столбце \(4\) больше по модулю, чем \(2\), поэтому меняем строки местами:

$$ PA= \begin{pmatrix} 4&3\\ 2&1 \end{pmatrix}, \qquad Pb= \begin{pmatrix} 11\\5 \end{pmatrix}. $$

Множитель исключения равен \(2/4=0{,}5\). Вычитаем из второй строки половину первой:

$$ (2,1)-0{,}5(4,3)=(0,-0{,}5). $$

Получили разложение:

$$ L= \begin{pmatrix} 1&0\\ 0{,}5&1 \end{pmatrix}, \qquad U= \begin{pmatrix} 4&3\\ 0&-0{,}5 \end{pmatrix}. $$

Первый этап: решаем \(Ly=Pb\).

$$ \begin{pmatrix} 1&0\\ 0{,}5&1 \end{pmatrix} \begin{pmatrix} y_1\\y_2 \end{pmatrix} = \begin{pmatrix} 11\\5 \end{pmatrix}. $$

Перепишем обычными уравнениями:

$$ \begin{cases} y_1=11,\\ 0{,}5y_1+y_2=5. \end{cases} $$

Из первой строки сразу знаем \(y_1=11\). Подставляем во вторую:

$$ y_2=5-0{,}5\cdot11=-0{,}5. $$

Здесь идём сверху вниз: каждое следующее уравнение использует уже найденные значения. Это называется прямой подстановкой.

Второй этап: решаем \(Ux=y\).

$$ \begin{pmatrix} 4&3\\ 0&-0{,}5 \end{pmatrix} \begin{pmatrix} x_1\\x_2 \end{pmatrix} = \begin{pmatrix} 11\\-0{,}5 \end{pmatrix}. $$

То есть:

$$ \begin{cases} 4x_1+3x_2=11,\\ -0{,}5x_2=-0{,}5. \end{cases} $$

Теперь начинаем снизу:

$$ x_2=1, \qquad x_1=\frac{11-3\cdot1}{4}=2. $$

Это обратная подстановка. Решение исходной системы:

$$ \boxed{x_1=2,\qquad x_2=1}. $$

Смысл LU-разложения здесь такой: одну исходную систему заменили двумя треугольными, в которых неизвестные легко находить по очереди. А если изменится только \(b\), разложение матрицы заново делать не понадобится — используем те же \(P,L,U\).

Начнём с определителя через LU — это небольшой шаг.

Для матрицы \(2\times2\) ты, возможно, помнишь:

$$ \det \begin{pmatrix} a&b\\ c&d \end{pmatrix} =ad-bc. $$

Но для большой матрицы так просто уже не получится. LU-разложение позволяет вычислить определитель через диагональ \(U\).

Нужны три свойства:

У треугольной матрицы определитель равен произведению диагональных элементов.
Определитель произведения матриц равен произведению их определителей: \(\det(LU)=\det L\cdot\det U\).
Каждый обмен двух строк меняет знак определителя.

У нашей \(L\) на диагонали единицы, поэтому:

$$ \det L=1. $$

Значит, если перестановок не было, из \(A=LU\) получаем:

$$ \det A=\det U=u_{11}u_{22}\cdots u_{nn}. $$

Если перестановки были, учитываем их число \(s\):

$$ \boxed{\det A=(-1)^s\,u_{11}u_{22}\cdots u_{nn}}. $$

То есть перемножаем диагональные элементы \(U\), а при нечётном числе обменов меняем знак результата.

Проверим на нашем примере:

$$ A= \begin{pmatrix} 2&1\\ 4&3 \end{pmatrix}, \qquad U= \begin{pmatrix} 4&3\\ 0&-0{,}5 \end{pmatrix}. $$

Мы один раз поменяли строки местами, поэтому \(s=1\). Получаем:

$$ \det A=(-1)\cdot4\cdot(-0{,}5)=2. $$

Проверка обычной формулой:

$$ \det A=2\cdot3-1\cdot4=2. $$

Для чего нам вообще определитель здесь? Квадратная матрица имеет обратную, а система \(Ax=b\) — единственное решение для любого \(b\), именно когда \(\det A\ne0\). Если \(\det A=0\), обратной матрицы нет, а у системы может быть либо бесконечно много решений, либо ни одного.

Теперь — как найти обратную матрицу через LU-разложение. Здесь пригодится то, что мы уже умеем: решать системы с разными правыми частями.

Что такое обратная матрица?

Для числа \(5\) обратное число — \(1/5\), потому что:

$$ 5\cdot\frac15=1. $$

У матрицы похожая идея:

$$ A A^{-1}=E, $$

где \(E\) — единичная матрица:

$$ E= \begin{pmatrix} 1&0\\ 0&1 \end{pmatrix}. $$

При умножении она играет роль единицы: \(AE=EA=A\). А запись \(A^{-1}\) обозначает обратную матрицу — это не деление единицы на каждый элемент \(A\).

Как её искать?

Обозначим неизвестную обратную матрицу через \(B\):

$$ B= \begin{pmatrix} b_{11}&b_{12}\\ b_{21}&b_{22} \end{pmatrix}. $$

Нужно добиться:

$$ AB=E. $$

При умножении \(A\) на \(B\) каждый столбец результата получается умножением \(A\) на соответствующий столбец \(B\). Поэтому требование \(AB=E\) разбивается на две системы:

$$ A \begin{pmatrix} b_{11}\\b_{21} \end{pmatrix} = \begin{pmatrix} 1\\0 \end{pmatrix}, \qquad A \begin{pmatrix} b_{12}\\b_{22} \end{pmatrix} = \begin{pmatrix} 0\\1 \end{pmatrix}. $$

Получается простой способ:

Решаем систему с первым столбцом единичной матрицы справа — получаем первый столбец обратной. Затем решаем со вторым — получаем второй столбец обратной.

Посмотрим на знакомой матрице:

$$ A= \begin{pmatrix} 2&1\\ 4&3 \end{pmatrix}. $$

Её разложение мы уже нашли:

$$ P= \begin{pmatrix} 0&1\\ 1&0 \end{pmatrix}, \quad L= \begin{pmatrix} 1&0\\ 0{,}5&1 \end{pmatrix}, \quad U= \begin{pmatrix} 4&3\\ 0&-0{,}5 \end{pmatrix}. $$

Найдём первый столбец обратной матрицы.

Решаем \(Az=e_1\), где \(e_1=(1,0)^T\). Буквой \(z\) обозначим искомый столбец, а запись \(T\) здесь превращает строку в столбец.

Как и раньше, сначала решаем \(Ly=Pe_1\). Перестановка меняет элементы правой части местами:

$$ Pe_1= \begin{pmatrix} 0\\1 \end{pmatrix}. $$

Следовательно:

$$ \begin{cases} y_1=0,\\ 0{,}5y_1+y_2=1, \end{cases} \quad\Rightarrow\quad y= \begin{pmatrix} 0\\1 \end{pmatrix}. $$

Затем решаем \(Uz=y\):

$$ \begin{cases} 4z_1+3z_2=0,\\ -0{,}5z_2=1. \end{cases} $$

Снизу вверх:

$$ z_2=-2,\qquad z_1=\frac{0-3(-2)}4=1{,}5. $$

Первый столбец обратной — \((1{,}5,-2)^T\).

Для второго столбца точно так же используем правую часть \(e_2=(0,1)^T\). После перестановки \(Pe_2=(1,0)^T\):

$$ Ly=Pe_2 \quad\Rightarrow\quad y_1=1,\quad y_2=-0{,}5. $$

Затем:

$$ Uz=y \quad\Rightarrow\quad z_2=1,\quad z_1=\frac{1-3}{4}=-0{,}5. $$

Собираем найденные решения по столбцам:

$$ \boxed{ A^{-1}= \begin{pmatrix} 1{,}5&-0{,}5\\ -2&1 \end{pmatrix} }. $$

Проверяем:

$$ AA^{-1} = \begin{pmatrix} 2&1\\ 4&3 \end{pmatrix} \begin{pmatrix} 1{,}5&-0{,}5\\ -2&1 \end{pmatrix} = \begin{pmatrix} 3-2&-1+1\\ 6-6&-2+3 \end{pmatrix} = E. $$

Для матрицы размера \(n\times n\) решаем \(n\) систем, по одной на каждый столбец обратной. При этом LU-разложение делаем только один раз: матрица \(A\) во всех системах одна и та же.

Теперь закроем одну важную деталь LU: что делать с уже записанными множителями в \(L\), если перестановка понадобилась посреди разложения. Мы обещали к этому вернуться.

Возьмём матрицу:

$$ A= \begin{pmatrix} 4&0&1\\ 2&1&0\\ 1&3&2 \end{pmatrix}. $$

В первом столбце наибольший по модулю элемент \(4\) уже сверху. Перестановка не нужна.

Обнуляем элементы под ним:

Из второй строки вычитаем первую, умноженную на \(2/4=0{,}5\).
Из третьей строки вычитаем первую, умноженную на \(1/4=0{,}25\).

Получаем:

$$ U= \begin{pmatrix} 4&0&1\\ 0&1&-0{,}5\\ 0&3&1{,}75 \end{pmatrix}, \qquad L= \begin{pmatrix} 1&0&0\\ \boxed{0{,}5}&1&0\\ \boxed{0{,}25}&0&1 \end{pmatrix}. $$

Переходим ко второму столбцу. Среди оставшихся строк выбираем между \(1\) и \(3\). Опорным должен стать \(3\), поэтому меняем вторую и третью строки \(U\):

$$ U= \begin{pmatrix} 4&0&1\\ 0&3&1{,}75\\ 0&1&-0{,}5 \end{pmatrix}. $$

Но эти строки уже имеют свою «историю»:

Для строки, которая теперь стала второй, раньше использовали множитель \(0{,}25\).
Для строки, которая теперь стала третьей, использовали \(0{,}5\).

Поэтому эти ранее записанные множители в \(L\) тоже нужно поменять местами:

$$ L= \begin{pmatrix} 1&0&0\\ \boxed{0{,}25}&1&0\\ \boxed{0{,}5}&0&1 \end{pmatrix}. $$

В \(L\) переставляем только уже заполненную часть слева от текущего столбца. Всю строку \(L\) менять нельзя: единицы на диагонали остаются на своих местах.

Теперь продолжаем исключение во втором столбце:

$$ m_{32}=\frac13. $$

Вычитаем из третьей строки \(U\) вторую, умноженную на \(1/3\), а множитель записываем в \(L_{32}\):

$$ U= \begin{pmatrix} 4&0&1\\ 0&3&\frac74\\ 0&0&-\frac{13}{12} \end{pmatrix}, \qquad L= \begin{pmatrix} 1&0&0\\ \frac14&1&0\\ \frac12&\frac13&1 \end{pmatrix}. $$

Матрица \(P\) фиксирует обмен второй и третьей строк:

$$ P= \begin{pmatrix} 1&0&0\\ 0&0&1\\ 0&1&0 \end{pmatrix}. $$

И результат удовлетворяет требуемому равенству:

$$ LU=PA= \begin{pmatrix} 4&0&1\\ 1&3&2\\ 2&1&0 \end{pmatrix}. $$

Правило для будущего кода: при обмене строк меняем целые строки в \(U\) и \(P\), а в \(L\) — только множители из предыдущих столбцов.

Закроем LU последним важным вопросом: **когда разложение не позволяет получить единственное решение?**

На очередном шаге мы ищем максимальный по модулю элемент в текущем столбце:

(⋯akkak+1,k⋮).\begin{pmatrix} \cdots\\ a_{kk}\\ a_{k+1,k}\\ \vdots \end{pmatrix}.

Если **все кандидаты равны нулю**, выбрать опорный элемент невозможно. Например:

A=(1224).A= \begin{pmatrix} 1&2\\ 2&4 \end{pmatrix}.

После исключения:

(2,4)−2(1,2)=(0,0),(2,4)-2(1,2)=(0,0),

поэтому

U=(1200).U= \begin{pmatrix} 1&2\\ 0&0 \end{pmatrix}.

На диагонали появился ноль:

det⁡A=1⋅0=0.\det A=1\cdot0=0.

Такая матрица называется **вырожденной**. У неё:

- нет обратной матрицы;
- система не имеет единственного решения;
- в зависимости от bb решений либо нет, либо бесконечно много.

Например:

{x1+2x2=3,2x1+4x2=6\begin{cases} x_1+2x_2=3,\\ 2x_1+4x_2=6 \end{cases}

имеет бесконечно много решений: второе уравнение просто повторяет первое.

А система

{x1+2x2=3,2x1+4x2=7\begin{cases} x_1+2x_2=3,\\ 2x_1+4x_2=7 \end{cases}

решений не имеет: левые части пропорциональны, а правые — нет.

В программе числа представлены приближённо, поэтому проверять `pivot == 0` ненадёжно. Используем малый порог, например:

```
epsilon = 1e-12

if abs(pivot) < epsilon:
    raise ValueError("Матрица вырожденная или близка к вырожденной")
```

`1e-12` означает:

10−12=0,000000000001.10^{-12}=0{,}000000000001.

То есть очень маленький опорный элемент считаем практически нулевым.

Теперь вся логика LU собрана:

1. Начинаем с U=AU=A, L=EL=E, P=EP=E.
2. В текущем столбце выбираем максимальный по модулю опорный элемент.
3. При необходимости переставляем строки в UU и PP, а в LL — ранее записанные множители.
4. Обнуляем элементы под опорным и сохраняем множители в LL.
5. Получаем PA=LUPA=LU.
6. Решаем Ly=PbLy=Pb, затем Ux=yUx=y.
7. Через те же L,U,PL,U,P находим определитель и обратную матрицу.
8. Проверяем LU≈PALU\approx PA, AA−1≈EAA^{-1}\approx E и невязку Ax−bAx-b.

По затратам:

- само LU-разложение требует порядка n3n^3 операций;
- решение одной системы после разложения — порядка n2n^2;
- разложение выгодно использовать повторно для разных правых частей.

Этого теоретически достаточно для пункта **1.1**. К реализации вернёмся после всей теории.


## 1.2 Метод прогонки

Переходим к методу прогонки. Здесь начну с проверки того, что у тебя уже осталось.

Дана трёхдиагональная система:

$$ \begin{cases} b_1x_1+c_1x_2=d_1,\\ a_2x_1+b_2x_2+c_2x_3=d_2,\\ a_3x_2+b_3x_3+c_3x_4=d_3,\\ a_4x_3+b_4x_4=d_4. \end{cases} $$

Мы храним только четыре одномерных массива:

$$ a=(a_2,a_3,a_4),\quad b=(b_1,b_2,b_3,b_4),\quad c=(c_1,c_2,c_3),\quad d=(d_1,d_2,d_3,d_4). $$

Вся матрица здесь не нужна.

Возьмём общий вид:

$$ \begin{cases} b_1x_1+c_1x_2=d_1,\\ a_2x_1+b_2x_2+c_2x_3=d_2,\\ a_3x_2+b_3x_3+c_3x_4=d_3,\\ \ldots\\ a_nx_{n-1}+b_nx_n=d_n. \end{cases} $$

Начинаем с первого уравнения:

$$ b_1x_1+c_1x_2=d_1. $$

Выражаем \(x_1\):

$$ b_1x_1=d_1-c_1x_2, $$ $$ x_1=-\frac{c_1}{b_1}x_2+\frac{d_1}{b_1}. $$

Сравниваем с нужной формой:

$$ x_1=\alpha_1x_2+\beta_1. $$

Получаем первые коэффициенты:

$$ \boxed{\alpha_1=-\frac{c_1}{b_1}}, \qquad \boxed{\beta_1=\frac{d_1}{b_1}}. $$

Вот почему первое уравнение особенное: для запуска алгоритма нам не нужны предыдущие коэффициенты — их ещё нет.

Теперь берём второе уравнение:

$$ a_2x_1+b_2x_2+c_2x_3=d_2. $$

Мы уже знаем зависимость:

$$ x_1=\alpha_1x_2+\beta_1. $$

Подставляем её вместо \(x_1\):

$$ a_2(\alpha_1x_2+\beta_1)+b_2x_2+c_2x_3=d_2. $$

Раскрываем скобки:

$$ a_2\alpha_1x_2+a_2\beta_1+b_2x_2+c_2x_3=d_2. $$

Собираем всё с \(x_2\):

$$ (b_2+a_2\alpha_1)x_2 = d_2-a_2\beta_1-c_2x_3. $$

Делим:

$$ x_2= -\frac{c_2}{b_2+a_2\alpha_1}x_3 + \frac{d_2-a_2\beta_1}{b_2+a_2\alpha_1}. $$

Мы снова получили нужный вид:

$$ x_2=\alpha_2x_3+\beta_2, $$

где

$$ \boxed{ \alpha_2=-\frac{c_2}{b_2+a_2\alpha_1} }, $$ $$ \boxed{ \beta_2= \frac{d_2-a_2\beta_1} {b_2+a_2\alpha_1} }. $$

Для произвольной строки формулы такие:

$$ \boxed{ \alpha_i=-\frac{c_i}{b_i+a_i\alpha_{i-1}} } $$

и

$$ \boxed{ \beta_i= \frac{d_i-a_i\beta_{i-1}} {b_i+a_i\alpha_{i-1}} }. $$

Это прямой ход: идём от первой строки к последней и последовательно вычисляем \(\alpha_i,\beta_i\).

Теперь последнее уравнение:

$$ a_nx_{n-1}+b_nx_n=d_n. $$

К этому моменту уже известно:

$$ x_{n-1}=\alpha_{n-1}x_n+\beta_{n-1}. $$

Подставляем:

$$ a_n(\alpha_{n-1}x_n+\beta_{n-1})+b_nx_n=d_n. $$

Отсюда сразу находится \(x_n\):

$$ \boxed{ x_n= \frac{d_n-a_n\beta_{n-1}} {b_n+a_n\alpha_{n-1}} }. $$

Последнее уравнение особенное, потому что справа от \(x_n\) уже нет следующей неизвестной. Оно обрывает цепочку и даёт конкретное число.

После этого начинается обратный ход:

$$ x_{n-1}=\alpha_{n-1}x_n+\beta_{n-1}, $$ $$ x_{n-2}=\alpha_{n-2}x_{n-1}+\beta_{n-2}, $$

и так далее до \(x_1\).

Весь метод можно увидеть как цепочку:

$$ x_1\rightarrow x_2\rightarrow x_3\rightarrow\ldots\rightarrow x_n $$

На прямом ходе мы не находим сами \(x_i\), а только сохраняем зависимости. Последняя строка даёт \(x_n\), после чего цепочка раскручивается назад.

Закроем прогонку условиями применимости и пойдём к следующей теме.

Метод прогонки применяется только к трёхдиагональной системе:

$$ a_i x_{i-1}+b_i x_i+c_i x_{i+1}=d_i. $$

То есть в каждой строке могут быть ненулевыми только:

элемент под главной диагональю \(a_i\);
элемент главной диагонали \(b_i\);
элемент над главной диагональю \(c_i\).

Например:

$$ \begin{pmatrix} b_1&c_1&0&0\\ a_2&b_2&c_2&0\\ 0&a_3&b_3&c_3\\ 0&0&a_4&b_4 \end{pmatrix}. $$

Поэтому всю матрицу хранить запрещено и бессмысленно. Храним только четыре массива:

lowerDiagonal = [a2, a3, a4]
mainDiagonal = [b1, b2, b3, b4]
upperDiagonal = [c1, c2, c3]
rightSide = [d1, d2, d3, d4]

В формулах постоянно встречается знаменатель:

$$ b_i+a_i\alpha_{i-1}. $$

Он не должен быть равен нулю, иначе очередной прогоночный коэффициент вычислить невозможно.

Удобное достаточное условие устойчивой работы — диагональное преобладание:

$$ \boxed{|b_i|\geq |a_i|+|c_i|} $$

То есть модуль элемента главной диагонали должен быть не меньше суммы модулей соседних элементов в этой строке. Желательно, чтобы хотя бы в одной строке неравенство было строгим.

Это достаточное, но не обязательное условие: если оно не выполнено, метод всё равно иногда работает. Просто заранее гарантировать хорошее поведение уже нельзя.

По скорости прогонка очень выгодна:

$$ \text{время } O(n),\qquad \text{память } O(n). $$

Мы один раз проходим вперёд и один раз назад. Для сравнения, обычный метод Гаусса для полной матрицы требует порядка \(O(n^3)\) операций.

На защите алгоритм можно описать так:

Метод прогонки предназначен для трёхдиагональных систем. На прямом ходе вычисляются коэффициенты зависимости \(x_i=\alpha_i x_{i+1}+\beta_i\). Последнее уравнение позволяет найти \(x_n\), после чего на обратном ходе последовательно находятся остальные неизвестные. Вся матрица не хранится, используются только три диагонали и правая часть.

Этого достаточно для теории пункта 1.2. В коде программа выведет все пары \(\alpha_i,\beta_i\) и итоговое решение.

## 1.3 МПИ и метод Зейделя

Переходим к методу простых итераций и методу Зейделя.

Оба решают систему

$$ Ax=b $$

приближённо. Сначала её преобразуют к виду:

$$ \boxed{x=Bx+c}. $$

Затем выбирают начальное приближение \(x^{(0)}\) и повторяют:

$$ \boxed{x^{(k+1)}=Bx^{(k)}+c}. $$

Например:

$$ \begin{cases} 10x_1+x_2=12,\\ 2x_1+10x_2=14. \end{cases} $$

Выразим каждую неизвестную из своего уравнения:

$$ x_1=1{,}2-0{,}1x_2, $$ $$ x_2=1{,}4-0{,}2x_1. $$

Если начать с

$$ x^{(0)}=(0,0), $$

то в методе простых итераций получим:

$$ x_1^{(1)}=1{,}2-0{,}1\cdot0=1{,}2, $$ $$ x_2^{(1)}=1{,}4-0{,}2\cdot0=1{,}4. $$

Главное правило МПИ: при вычислении всей новой итерации используются только значения предыдущей итерации.













































